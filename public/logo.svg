<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 420 120" fill="none">
  <!-- GOODBYE text with gloves in the O's -->
  <!-- G -->
  <text x="10" y="72" font-family="Arial Black, Arial, sans-serif" font-weight="900" font-size="72" fill="currentColor" letter-spacing="-2">G</text>
  <!-- O with boxing glove (left) -->
  <text x="60" y="72" font-family="Arial Black, Arial, sans-serif" font-weight="900" font-size="72" fill="currentColor" letter-spacing="-2">O</text>
  <!-- O with boxing glove (right) -->
  <text x="110" y="72" font-family="Arial Black, Arial, sans-serif" font-weight="900" font-size="72" fill="currentColor" letter-spacing="-2">O</text>
  <text x="160" y="72" font-family="Arial Black, Arial, sans-serif" font-weight="900" font-size="72" fill="currentColor" letter-spacing="-2">DBYE</text>
  <!-- Divider line and INVENTORY subtitle -->
  <text x="210" y="100" font-family="Arial, sans-serif" font-weight="700" font-size="16" fill="currentColor" text-anchor="middle" letter-spacing="6">— INVENTORY —</text>
</svg>
